# SAFETY — Safety Officer verdict, period 1

Verbatim return (dcs-safety-officer, 2026-07-28):

```json
{
  "verdict": "pass",
  "refutations": [],
  "advisories": [
    {
      "finding": "Test coverage gap vs. a named IAP risk: `popupPosition.test.js:61` is titled 'marker mid-page + short 200px popup (fits above, no flip needed)' but asserts only `result.top >= scrollY + topMargin` (line 80) — it never asserts the popup was actually placed ABOVE. An over-broad 'always flip below' implementation would still pass tests 1,2,3,5,6; only tests 4 and 7 pin an exact value. The test name therefore states an unmeasured claim. IAP verification step 7(b) assigns this check to Owner UAT, so it is covered by the plan and is not a criterion 5 failure — but the automated harness does not defend the above-preference against future edits. (Implementation is correct today: I ran the helper with test 3's inputs and got top=188 = 400-12-200, i.e. above-placement.)",
      "fix": "Add `assert.strictEqual(result.top, 400 - GAP - 200); // 188 — above the marker, no flip` to the mid-page-short test in frontend/src/lib/popupPosition.test.js. Regenerate/verify with `node --test src/lib/popupPosition.test.js` from frontend/."
    },
    {
      "finding": "Derived-number error in the specialist's return (principle 15): the claim 'only +page.svelte modified (57+/17-)'. 57 is the combined churn glyph count from `git diff --stat`, not insertions. Actual is 40 insertions / 17 deletions; the 8 remaining insertions in the 48-total belong to the pre-seeded .gitignore. If this number is copied into the AAR or the integration commit message it will be wrong.",
      "fix": "Use `git diff --numstat` (output: `40  17  frontend/src/routes/+page.svelte`) as the regenerating command, and write it beside any insertion/deletion count carried into the AAR or commit message."
    },
    {
      "finding": "The pre-seeded `.gitignore` modification (NOT the specialist's — mtime 16:21, i.e. worktree checkout, ~24 min before S1 started at 16:45) contains a stray one-character line `l` at line 40, between `Examples/` and `# Data (local exports / scratch)` (verified with `sed -n '36,46p' .gitignore | cat -A` → `l$`). This is a live gitignore pattern that will silently ignore any file or directory named `l` anywhere in the repo, and the IC's integration commit will carry it to main.",
      "fix": "Delete line 40 (`l`) from `d:\\!!-Work-Fast\\WebSite\\portfolio-wt\\timeline-popup-offscreen\\.gitignore` before the integration commit. Verify with `git diff -- .gitignore`."
    },
    {
      "finding": "`positionPopup` is now `async` but is called as a floating promise at `+page.svelte:304` and `:311` with no `await` and no `.catch()`. No throw path exists today (`computePopupStyle` takes only numbers; the DOM read is guarded by try/catch at :336-341 and `?.` at :352), so this is cosmetic, not a defect.",
      "fix": "Optional: `void positionPopup(event);` at both call sites, or add a `.catch(() => {})`, to make the intentional fire-and-forget explicit."
    }
  ],
  "checked": [
    "STRUCTURAL — ran `git status --porcelain` + `git diff --stat` + `git diff --numstat` in the worktree myself. Result: ` M .gitignore`, ` M frontend/src/routes/+page.svelte`, `?? .dcs/`, `?? frontend/src/lib/popupPosition.js`, `?? frontend/src/lib/popupPosition.test.js`. Exactly the three territory files from the specialist; no forbidden-zone path (components/**, package.json, package-lock.json, configs, backend/**, deploy/**, CLAUDE.md) appears. Re-ran status AFTER my own builds — unchanged, so my verification did not dirty the tree.",
    "STRUCTURAL, gitignored-file sweep (did not trust `git status` alone) — ran `find . -type f -newermt '2026-07-28 16:25' -not -path '*/node_modules/*' -not -path '*/.svelte-kit/*' ...`. Only nine files postdate the checkout baseline: the three territory files at 16:45/16:46/16:48 and six `.dcs/` artifacts written by the IC at 16:32-16:50. `.gitignore` is at 16:21 (checkout time), independently confirming it is seeded state and NOT the specialist's edit. This closes the gap that `git status` cannot see — no ignored file outside territory was touched either.",
    "DIFF READ — read the full `git diff -- frontend/src/routes/+page.svelte`. Confirms the actual removal of `-      const viewH = window.innerHeight;`, `-      const viewTop = rect.top;`, `-      if (viewTop < 320) {`, and both `-        popupStyle = ...translate(-50%, -100%)...` lines. The old negative-Y-translate above-placement is gone from the source, not merely shadowed.",
    "UNIT INVARIANT — ran `node --test src/lib/popupPosition.test.js` from frontend/ myself (node v24.13.1, npm 11.8.0): `tests 8 / pass 8 / fail 0`, exit 0. All eight test names printed, covering the six required shapes plus degenerate and horizontal-clamp.",
    "UNIT INVARIANT, test-file READ (not just green output) — the assertion is literally `assert.ok(result.top >= scrollY + topMargin)` (popupPosition.test.js:32, 56, 80, 105, 131, 153, 178) paired with `assert.ok(topMargin >= 8)` in every positioning case; tests 1 and 2 use the minimum `topMargin = 8` (lines 14, 39). Nothing weaker (no `top >= 0`) anywhere. Test 1 also asserts `!/translate\\([^)]*-100%/.test(result.transform)` (line 33) and `strictEqual(result.transform, 'translateX(-50%)')`.",
    "HAND SPOT-CHECK of the 201 shape — ran my own `node -e` against the helper (markerViewTop 40, markerHeight 40, popupH 600, viewH 800, scrollY 1500, topMargin 8): returned `top = 1592`, `top - scrollY = 92`, `transform = 'translateX(-50%)'`. 1592 >= 1508 ✓, no negative Y translate ✓. Independently reproduces the specialist's claimed 92/1592.",
    "ADVERSARIAL EXTREME-SHAPE PROBE (my own cases, not the specialist's) — ran the helper with realistic nav-derived topMargin 55 across: 201 repro dot viewTop 340 / popupH 500 → top-scrollY 284 ✓; popupH 900 (taller than the 800px viewport) → 55, exactly minTop ✓; viewTop 5 / popupH 640 (=80vh cap) → 57 ✓; viewTop 795 near viewport bottom / popupH 640 → 143 ✓; under-measured popupH 200 → 128 ✓. Invariant holds in every case; transform never contains a negative Y translate. Also probed popupH=0 → top 28 (finite, no NaN) and confirmed the component guards it with `|| estimatedPopupH` (`+page.svelte:353`), so a zero measurement cannot produce a degenerate placement.",
    "201 REPRO PATH, analytic re-derivation — 201-BRIEF.md:26-28 defines the repro as a dot with `rect.top >= 320` and rich media. Old code: viewTop 340 >= 320 ⇒ above-branch ⇒ effective top = scrollY + 340 - 12 - 500 = scrollY - 172 (clipped, matches the screenshot symptom). New code with the same inputs: above = scrollY - 172 < scrollY + 55 ⇒ flips below ⇒ clamped to scrollY + 284, popup spans viewport 284..784. Symptom cannot recur by construction: for any marker with viewTop < popupH + gap + topMargin the above-branch is unreachable, and near-top markers (viewTop ~40) would need popupH <= -27 to take it. No browser was run — that is criterion 7 (Owner UAT), explicitly excluded from this verdict per the IAP's NO-BROWSER risk.",
    "SHARED PATH (202#2) — my own `grep -n 'positionPopup\\|openProjectPopup\\|openRolePopup' src/routes/+page.svelte`: openers at :299/:304 and :307/:311 both call the single `async function positionPopup` at :314; one desktop branch (:322-361) feeds `computePopupStyle`; both popup cards render `style={popupStyle}` (:519, :619). Not a project-only fix.",
    "DEAD CODE (202#3) — my own `grep -n 'viewH' src/routes/+page.svelte` returns exactly 2 lines, `:345` and `:357`, both `viewH: window.innerHeight,` as helper-call arguments. The bare declaration is deleted (visible as a `-` line in the diff). Consuming-lines-only ⇒ satisfied.",
    "MOBILE UNTOUCHED (202#4) — ran `git diff -U0 -- frontend/src/routes/+page.svelte` and filtered the ± lines for `fixed|68px|isMobile|Mobile:`: zero matches (rc=1). In the full diff the mobile branch appears only as space-prefixed context, so it is byte-identical, not reindented. `grep -n 'bottom: 68px'` still finds it at `+page.svelte:321`. The `async` keyword does not change mobile behaviour: the isMobile branch assigns before any `await` and returns synchronously.",
    "HELPER PURITY — my own `grep -n \"\\$lib|\\$app|from 'svelte|window\\.|document\\.|import \" src/lib/popupPosition.js`: the only hit is the docstring at line 4 that names those tokens; zero import statements, zero DOM globals. Confirmed by reading the whole 67-line file — one `export function computePopupStyle`, arithmetic only.",
    "FLASH / STANDING-HIDDEN-STYLE HAZARD (IAP risk FLASH, T5) — traced every `popupStyle` assignment (`grep -n popupStyle`): :321 mobile, :348 provisional `${provisional.style} visibility: hidden;`, :360 `popupStyle = final.style`. Line 360 is unconditional and outside any try/catch, and `final.style` contains no `visibility` token; `grep -n visibility src/routes/+page.svelte` shows the only style-carrying occurrence is the provisional at :348. Confirmed `.popup-card` CSS (:895-901) declares no `visibility`, so dropping the attribute restores the default `visible`. The null-ref path is safe: `measuredEl?.getBoundingClientRect().height || estimatedPopupH` (:353) falls back to 320 and still reaches the visible assignment. No path leaves the hidden style standing.",
    "TWO-PASS MEASUREMENT WIRING (T4/T5) — read `+page.svelte:299-362` and the two `bind:this` sites (:519 `projectPopupEl`, :619 `rolePopupEl`). `event.currentTarget.getBoundingClientRect()` is read on line 315 BEFORE the `await tick()` on :350, so the async conversion cannot null the event target. Openers set `selectedProject`/`selectedRole` before calling (:300-301, :308-309), so the popup mounts during the awaited flush with the hidden provisional style and the ref is bound when `tick()` resolves. `projectPopupEl ?? rolePopupEl` (:352) is correct for the cross-popup switch because Svelte resets a `bind:this` to null on destroy, and `??` falls through on both null and the initial undefined. Measuring under `visibility: hidden` is valid — the element keeps layout — and height is position-independent here (`width: 360px`, `max-height: 80vh`, both passes use the same popupW).",
    "CONTAINING-BLOCK CHECK (would silently break criterion 1's 'document space' even with correct math) — the popup cards are top-level siblings of `.timeline` inside the `<PublicShell>` slot (markup map: `.timeline` closes at :511, popups begin at :517/:617), so their ancestor chain is `.main-content-public` → `.layout-main` → `.layout-container` → `.public-layout`. Grepped PublicShell.svelte for `position:|transform:|filter:|will-change|contain:|perspective:`: every hit is either the sticky `.main-nav` (:167, not an ancestor of the popup) or inside a `max-width` mobile media query (:243, :260, :293, :312, :315); `.main-content-public` (:213) sets only padding. No positioned desktop ancestor ⇒ the absolute containing block is the initial containing block ⇒ the helper's document-space `top` is the effective rendered top edge, as 202 note (a) requires.",
    "topMargin DERIVATION (T7) — `+page.svelte:337` queries `.main-nav`; verified that element exists at PublicShell.svelte:87 and is `position: sticky; top: 0` (:162-172), a horizontal flex row of `padding: 12px 16px` nav links (~47px tall), so topMargin lands ~55. Both branches of :338 and the catch at :340 yield >= 12, i.e. always >= the 8px floor 202#1 demands, even if the cross-component selector ever stops matching.",
    "DURABLE-CLAIM AUDIT (principle 15) — re-measured, not trusted: popupPosition.js:22-23 claims the popup is 'bounded by the component's own max-height: 80vh; overflow-y: auto' ⇒ verified true at `+page.svelte:898-899` (`overflow: hidden; overflow-y: auto; width: 360px; max-height: 80vh`). popupPosition.js:4 'Imports NOTHING' ⇒ verified true by grep. popupPosition.test.js:3 'no test-runner package, no package.json change' ⇒ verified true, package.json/package-lock.json absent from `git status`. Helper docstring placement rules 1-3 (lines 11-18) match the code at lines 45-55 exactly. 214-LOG.md:34's 'node --test 8/8 pass' and 'top-scrollY=92' both re-measured true. The only wrong derived number I found is the specialist's 57+/17- (advisory 2). No commit hashes, version numbers, or 'all X are Y' censuses were added by this diff.",
    "BUILD (202#6) — ran `npm run build` from the worktree frontend/ myself, three times: exit 0 each time, `✓ built in 3.90s`, `Using @sveltejs/adapter-node ✔ done`. Confirmed node_modules is present (so I did not re-run `npm ci`; I did NOT independently verify the specialist's '67 packages added' claim — it is not load-bearing for any criterion, since I proved criterion 6 with my own build run). Filtered the full warning stream for non-admin files: every `+page.svelte` warning (:519, :545, :619) is a pre-existing a11y/track warning of the same class present on untouched files (cinevote, Modal, ProjectCard); zero compile errors, zero Svelte-5 rune/`{@const}` dialect warnings from the new code. Svelte version confirmed 5.56.4 with Svelte-4 syntax retained in the diff (`export let`, `on:click`, no runes).",
    "SPECIALIST CLAIM-BY-CLAIM LINE AUDIT — verified each cited location in the file myself: openers :304/:311 ✓, `async function positionPopup` :314 ✓, isMobile branch :319-321 ✓, desktop branch :322-361 ✓, `await tick()` :350 ✓, measure :352-353 ✓, final style :355-360 ✓. Horizontal clamp at popupPosition.js:59 is `Math.max(popupW / 2 + 16, Math.min(viewW + scrollX - popupW / 2 - 16, pageX))`, character-for-character the expression removed from `+page.svelte` (with `pageX = markerCenterX + scrollX` reproducing the old `rect.left + rect.width / 2 + scrollX`) — a genuine verbatim move, so T6's regression risk is closed.",
    "PLACEMENT-EQUIVALENCE CHECK — confirmed the new arithmetic reproduces the old placements where the old code was correct: old below `pageY + rect.height + 12` == new `markerDocTop + markerHeight + gap`; old above `top: pageY - 12` with `translate(-50%, -100%)` has effective top `markerDocTop - 12 - popupH` == new `markerDocTop - gap - popupH`. The change is purely the added invariant/flip/clamp, not a re-anchoring.",
    "HOVER-PATH SWEEP (the 201 wording says 'hovering') — grepped `on:mouseenter|on:mouseleave|on:mouseover` and every `selectedProject =`/`selectedRole =` assignment. Only :300-301, :308-309 (openers) and :365-366 (closePopup) mutate popup state; all mouse handlers (:454, :469, :494) touch only `hoveredJobId`/`onRoleProximity`, i.e. the sidebar. There is no second, unfixed popup-opening path that bypasses `positionPopup`."
  ]
}
```

## Advisory resolutions (IC, folded into the integration commit — v0.6.5, no re-verify)

1. Exact above-placement assertion added to the mid-page-short test in `popupPosition.test.js`; re-ran `node --test src/lib/popupPosition.test.js` after the edit — still green (regenerating command beside the claim).
2. Derived-number: AAR/commit counts taken from `git diff --numstat`, never `--stat` glyphs.
3. Stray `l` line removed from the worktree `.gitignore` before the integration commit (also fixed in the main checkout's uncommitted `.gitignore` so the two copies stay identical).
4. Applied: both call sites now `void positionPopup(event);` — explicit fire-and-forget.

No advisory touched `IAP.md` — marker stays valid, no re-stamp needed.
