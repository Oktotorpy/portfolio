# 204 — Tasking S1

**Incident:** timeline-popup-offscreen
**Period:** 1
**Specialist:** dcs-ops-specialist (S1)

## Task

Fix desktop popup vertical placement on the timeline homepage — covers 202 criteria 1, 2, 3, 4, 5, 6 (criterion 7 is Owner UAT, not yours).

1. Create the pure helper `frontend/src/lib/popupPosition.js` per tactics T1/T2/T3/T6: one exported function taking marker rect + scroll + viewport + popup dimensions + margins and returning an explicit document-space `top`, a horizontally-clamped `left`, `transform: translateX(-50%)` only (never a negative Y translate), and the assembled style string. It must import nothing — no `$lib`, `$app`, `svelte`, or DOM globals — so plain Node can import it with no node_modules present. Placement rule: prefer above the marker, flip below when above would violate the invariant, then clamp `top` into [scrollY + topMargin, max(scrollY + topMargin, scrollY + viewH - popupH - bottomMargin)]; when the viewport is too short for both bounds, the top invariant wins (202#1).
2. Create `frontend/src/lib/popupPosition.test.js` using only `node:test` + `node:assert` (no framework, no package.json change) asserting `top >= scrollY + topMargin` and `topMargin >= 8` across at least these cases (202#5): marker near page top + tall 600px popup, unscrolled (the 201 regression shape); same but scrolled (scrollY 1500); marker mid-page + short 200px popup; marker mid-page + tall 600px popup in an 800px viewport; marker near viewport bottom, short and tall; and the degenerate case popupH > viewH - topMargin - bottomMargin. Add one case asserting the horizontal clamp still yields left within [popupW/2+16, viewW+scrollX-popupW/2-16] (unchanged behaviour, T6).
3. Rewire `positionPopup` in `frontend/src/routes/+page.svelte` (currently lines 311-339) per T4/T5/T7/T8: keep the `isMobile` fixed-bottom branch byte-identical and textually first (202#4 — do not reindent or reflow it); make the desktop branch two-pass (provisional `visibility: hidden` style → `await tick()` → measure `getBoundingClientRect().height` off a per-popup `bind:this` ref → final style from the same helper), pass a `topMargin` derived from `.main-nav` height with a fallback, and either delete `viewH` or feed it to the helper as a real input (202#3). Leave `openProjectPopup` (line 296) and `openRolePopup` (line 304) both routing through `positionPopup` (202#2).
4. Prove the app still builds (202#6): `npm ci` then `npm run build` in the worktree's frontend/ — note the directory currently has NO node_modules, so `npm ci` needs npm-registry access; if the harness is offline, STOP and return a deviation with the exact npm error instead of claiming or skipping criterion 6.

Do not add dependencies, do not touch PublicShell.svelte / ProjectCard.svelte / package.json / package-lock.json / configs, and stay in Svelte 4 syntax (no runes; no `{@const}` outside a control-flow block — CLAUDE.md pitfalls 1 and 6). All commands run from the worktree root d:\!!-Work-Fast\WebSite\portfolio-wt\timeline-popup-offscreen (or its frontend/ subdir where noted).

Tactics referenced above (from the Planning Chief, verbatim intent):
- T1 — framework-free pure module exporting one function, e.g. `computePopupStyle({ markerViewTop, markerHeight, markerCenterX, scrollX, scrollY, viewW, viewH, popupW, popupH, gap, topMargin, bottomMargin })` returning `{ position, top, left, transform, maxWidth, style }`, `top` document-space, transform `translateX(-50%)` only.
- T2 — measured-height placement: prefer above (`top = markerDocTop - gap - popupH`); if top invariant violated, flip below (`top = markerDocTop + markerHeight + gap`); hard-clamp into [scrollY + topMargin, max(scrollY + topMargin, scrollY + viewH - popupH - bottomMargin)]; lower bound wins when viewport too short.
- T3 — always explicit document-space `top`, never `translate(-50%, -100%)` — post-measurement content growth then expands downward only (bounded by existing `max-height: 80vh; overflow-y: auto`).
- T4 — two-pass render in component: provisional style with `visibility: hidden` (estimated height) → `await tick()` → measure real height via `bind:this` ref → recompute with same helper → final visible style. `positionPopup` may become async; openers keep calling it as-is.
- T5 — two separate refs (`projectPopupEl`, `rolePopupEl`) across the two `{#if}` popup blocks (~lines 496 and 596); measure `projectPopupEl ?? rolePopupEl`; if both null, still assign a visible style using the estimated height (never leave the provisional hidden style standing).
- T6 — move the existing horizontal clamp (`Math.max(popupW/2+16, Math.min(viewW+scrollX-popupW/2-16, pageX))`) into the helper unchanged; keep the `isMobile` early return in the component, textually ahead of the helper call.
- T7 — `topMargin` from sticky nav: `const navH = document.querySelector('.main-nav')?.getBoundingClientRect().height ?? 0; const topMargin = navH + 8;` fallback 12 if query throws or returns 0. Helper stays pure — DOM read in component, passed as a number.
- T8 — `viewH` either deleted or fed to the helper as a real input; never declared-and-unused.
- T9 — Svelte 4 syntax on Svelte 5 compiler; no new dependency; no test-runner install; no package.json / package-lock.json / vite.config.js / svelte.config.js change.

## File territory (may edit only within these globs)

- frontend/src/routes/+page.svelte
- frontend/src/lib/popupPosition.js
- frontend/src/lib/popupPosition.test.js

## Forbidden zones (explicitly, even if it seems related)

- frontend/src/lib/components/**
- frontend/src/lib/api.js
- frontend/src/lib/stores.js
- frontend/src/lib/utils.js
- frontend/src/lib/data.server.js
- frontend/src/params/**
- frontend/src/routes/**/+page.server.js
- frontend/src/routes/[slug=notadmin]/**
- frontend/src/routes/admin/**
- frontend/src/routes/+layout.svelte
- frontend/src/app.css
- frontend/package.json
- frontend/package-lock.json
- frontend/svelte.config.js
- frontend/vite.config.js
- backend/**
- deploy/**
- CLAUDE.md
- .dcs/**

## Evidence required in the return

- From frontend/: `node --version && npm --version` — paste both lines (expect node v24.x; if a v18 shell is resolved, switch via nvm before building and say so).
- From frontend/: `node --test src/lib/popupPosition.test.js` — paste the test-name lines plus the `# tests / # pass / # fail` summary. Must show 0 failures and must visibly include the six 202#5 shapes (marker-near-top+tall unscrolled, marker-near-top+tall scrolled, mid-page+short, mid-page+tall, near-viewport-bottom, degenerate popupH > viewH). Run with the explicit file path, not bare `node --test`, so the runner does not walk node_modules.
- From frontend/: `grep -n "viewH" src/routes/+page.svelte` — paste the raw output. 202#3 passes only if it is empty or every line shown genuinely consumes the value (assignment to a helper argument counts; a bare declaration does not).
- From frontend/: `grep -n "positionPopup\|openProjectPopup\|openRolePopup" src/routes/+page.svelte` — paste output showing both openers still call the single shared positionPopup (202#2).
- From the worktree root: `git diff -U0 -- frontend/src/routes/+page.svelte | grep -E "^[-+].*(bottom: 68px|position: fixed; bottom)"` — must print nothing, i.e. the diff contains no added or removed line touching the mobile fixed-bottom branch (202#4). Pair it with `grep -n "bottom: 68px" frontend/src/routes/+page.svelte` showing the line still exists.
- From frontend/: `grep -nE "\$lib|\$app|from ['\"]svelte|window\.|document\." src/lib/popupPosition.js` — must print nothing, proving the helper is framework-free and plain-Node importable (the precondition that makes 202#5 runnable in this harness).
- From frontend/: `npm ci` then `npm run build` — paste the last ~10 lines of build output including the adapter-node summary, plus the shell exit code of the build command (202#6). If `npm ci` fails for network reasons, return a deviation instead of a pass.
- From the worktree root: `git status --porcelain` and `git diff --stat` — must list only the three territory files (plus untracked node_modules/build, which are gitignored), proving no forbidden path was touched.
- The decisive excerpt only, not a full paste: the final `positionPopup` body as `frontend/src/routes/+page.svelte:<start>-<end>` with the ~20 lines of the desktop branch, and the clamp expression from `popupPosition.js` with its `file:line`. One sentence stating which placement the 201 repro shape (marker near page top, 600px popup) resolves to and what `top - scrollY` equals for it.

## On discovering the plan doesn't fit reality

STOP. Do not improvise a different fix. Return `status: "deviation"` per
`references/schemas.md` #4 (ops-specialist return), with `found`,
`why_plan_wrong`, and a
`proposal` (a recommendation, not an action). The IC will re-enter
planning around your finding.
